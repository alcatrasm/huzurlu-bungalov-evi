
import React from 'react';
import Layout from '@/components/layout/Layout';
import StaticPageViewer from '@/components/static/StaticPageViewer';

const StaticPage = () => {
  return (
    <Layout>
      <StaticPageViewer />
    </Layout>
  );
};

export default StaticPage;
